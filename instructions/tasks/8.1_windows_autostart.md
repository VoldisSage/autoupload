# Task 8.1 – Windows autostart documentation

## Description
Provide documentation and optional helper scripts to ensure the StreamerTools AutoUploader process starts automatically when Windows starts.

## Definition of Done
- The project-level `README.md` is updated with a section "Windows autostart" explaining at least:
  - How to create a shortcut in the Windows Startup folder (`shell:startup`) that runs:
    - `node C:\ruta\al\proyecto\dist\index.js`
  - Alternatively, how to create a small `.bat` or `.ps1` script that launches the app.
- (Optional) A helper script is provided, such as:
  - `scripts/create-startup-shortcut.ps1` or a `.bat` file, with clear usage instructions.
- No code changes are strictly required in `src/`, unless a small convenience entrypoint is desired.

## Tests
- Manual test (documentation-driven):
  - Follow the README instructions on a Windows machine.
  - Reboot or log out/log in.
  - Verify that the AutoUploader process starts automatically (e.g. it logs to console or Discord at startup).

## Files to create / modify
- `README.md` (project-level)
- Optionally: `scripts/create-startup-shortcut.ps1` or `.bat` file
